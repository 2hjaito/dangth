services:
  dangth:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dangth-prod
    environment:
      - NODE_ENV=production
      - PORT=1210
    ports:
      - "127.0.0.1:1210:1210"
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
